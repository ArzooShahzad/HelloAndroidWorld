<?xml version="1.0" encoding="utf-8"?>
<!-- activity_main.xml file defines the layout for an activity's user interface (UI).
  I can customize attributes of widgets in this file.
  Whenever I adda a new widget in the Design view, it will appear here.
  As well as, it lists all the widgets that are listed in the component tree.
  This file has three views of Code, Split and Design respectively.
 -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Changed the android:text="@string/my_first_android_app"" -->
    <!-- Added the android:textSize="24dp" -->
    <!-- Added the android:textStyle="bold" -->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/my_first_android_app"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Added the ImageView -->
    <!-- Clicked on the ImageView widget and then within the Layout Constraint Widget clicked on
     the + sign for Top Margin, Left Margin, Right Margin respectively.
     -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="0dp"
        android:layout_height="250dp"
        android:layout_marginStart="68dp"
        android:layout_marginTop="59dp"
        android:layout_marginEnd="68dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/android"
        android:contentDescription="@string/android_image" />

</androidx.constraintlayout.widget.ConstraintLayout>